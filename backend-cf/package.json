{
  "name": "babylon-report-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute babylon-pokladnice --file=schema.sql",
    "db:init:remote": "wrangler d1 execute babylon-pokladnice --remote --file=schema.sql",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
